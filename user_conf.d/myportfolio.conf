map $request_method $timeline_post_limit_key {
    default "";
    POST $binary_remote_addr;
}

limit_req_zone $timeline_post_limit_key zone=timeline_post_limit:10m rate=1r/m;

server {
    listen 80;
    server_name heliadinh.duckdns.org;

    if ($host = heliadinh.duckdns.org) {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name heliadinh.duckdns.org;

    resolver 127.0.0.11 valid=30s;
    set $myportfolio_upstream http://myportfolio:5000;

    location = /api/timeline_post {
        limit_req zone=timeline_post_limit;
        limit_req_status 429;
        proxy_pass $myportfolio_upstream;
    }

    location / {
        proxy_pass $myportfolio_upstream;
    }

    # Load the certificate files.
    ssl_certificate /etc/letsencrypt/live/myportfolio/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/myportfolio/privkey.pem;
    ssl_trusted_certificate /etc/letsencrypt/live/myportfolio/chain.pem;
}
